# If you want to use a Docker Volume :
#   Uncomment the line "db_data:/var/lib/mysql" and comment the line "./.server_files/AAEmu.Database/mysql:/var/lib/mysql" that are in the db services
#   Also uncomment the two volumes line at the end... This is useful if you use WSL Shell on Windows to launch the compose file.
services:
 
    db:
        image: mysql:8.0.36
        restart: unless-stopped
        # command: --default_authentication_plugin=mysql_native_password
        volumes:
            - ./SQL/aaemu_game.sql:/docker-entrypoint-initdb.d/aaemu_game.sql
            - ./.server_files/AAEmu.Database/mysql:/var/lib/mysql
            # - db_data:/var/lib/mysql
        environment:
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
        ports:
            - 3306:3306
        healthcheck:
            test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p${DB_PASSWORD}" ]
            interval: 5s
            timeout: 5s
            retries: 20

    adminer:
        image: adminer
        restart: unless-stopped
        environment:
            ADMINER_DEFAULT_DB_DRIVER: mysql
            ADMINER_DEFAULT_DB_HOST: db
            ADMINER_DESIGN: nette
        ports:
            # 8081 on the host: the Go login server uses 8080 for its launcher API.
            - 8081:8080
        depends_on:
            db:
              condition: service_healthy

    # The login server is the Go binary from aaemu-cluster (server/cmd/login).
    # Run it on the host with its MySQL settings pointed at this db service
    # (host port 3306) and the Game listed in AAEMU_LOGIN_GAME_SERVERS; see
    # aaemu-cluster/server/README.md "Run locally". The Game reaches it
    # through host.docker.internal:1234, which extra_hosts maps to the host
    # gateway on Linux as well as Docker Desktop.
    game:
        build:
            context: .
            dockerfile: ./AAEmu.Game/Dockerfile
            args:
                - CONFIGURATION=${BUILD_CONFIGURATION}
                - FRAMEWORK=${BUILD_FRAMEWORK}
                - RUNTIME=${BUILD_RUNTIME}
                - DB_HOST=db
                - DB_PORT=3306
                - DB_USER=${DB_USER}
                - DB_PASSWORD=${DB_PASSWORD}
        image: aaemu-game:${PROJECT_VERSION_PREFIX}-${PROJECT_VERSION_SUFFIX}
        restart: unless-stopped
        extra_hosts:
            - "host.docker.internal:host-gateway"
        volumes:
          - "./.server_files/AAEmu.Game/ClientData:/app/ClientData:ro"
          - "./.server_files/AAEmu.Game/Data:/app/Data:ro"
          - "./.server_files/AAEmu.Game/Configurations:/app/Configurations:ro"
          - "./.server_files/AAEmu.Game/Config.json:/app/Config.json:ro"
        environment:
            DOTNET_SYSTEM_GLOBALIZATION_INVARIANT: 1
        ports:
            - 1239:1239
            - 1250:1250
        develop:
            watch:
                - path: ./AAEmu.Game
                  action: rebuild
        healthcheck:
            test: ["CMD-SHELL", "mysql aaemu_game -h db -u root -p${DB_PASSWORD}"]
            interval: 5s
            timeout: 5s
            retries: 20
        depends_on:
            db:
              condition: service_healthy
            
# volumes:
#   db_data: